/* chart.min.js placeholder - copy the real minified chart library here if desired */
(function(){
    // Minimal chart stub for pages that expect Chart to exist
    window.Chart = window.Chart || function(){ return {}; };
})();